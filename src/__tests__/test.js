// import * as data from '../../testData';

// import { agnes, diana } from '..';

describe.skip('Hierarchical clustering test', () => {
  it.skip('DIANA test', () => {
    // var clust = diana(data.features1);
    // expect(clust.distance).toBeCloseTo(3.136, 3);
    expect(true).toBe(true);
  });
});
